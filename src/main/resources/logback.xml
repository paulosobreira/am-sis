<configuration>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/am-sis.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/am-sis.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>70MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%logger{40}] %msg%n%ex</pattern>
        </encoder>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%logger{30}] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Frameworks internos: só WARN e acima para não poluir o log -->
    <logger name="org.hibernate"          level="WARN"/>
    <logger name="org.apache.tomcat"      level="WARN"/>
    <logger name="org.apache.catalina"    level="WARN"/>
    <logger name="org.apache.coyote"      level="WARN"/>
    <logger name="org.glassfish.jersey"   level="WARN"/>
    <logger name="com.sun.jersey"         level="WARN"/>
    <logger name="org.eclipse.birt"       level="WARN"/>
    <logger name="org.eclipse.equinox"    level="WARN"/>
    <logger name="org.eclipse.core"       level="WARN"/>
    <logger name="com.fasterxml.jackson"  level="WARN"/>
    <logger name="org.jboss"              level="WARN"/>

    <!-- Código da aplicação: DEBUG -->
    <logger name="br.com.am" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="FILE"/>
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
